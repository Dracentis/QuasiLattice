# QuasiLattice
QuasiLattice (or Lattice for short) is a data analysis, knowledge base, journaling and note taking system built on a simple RESTful API. This repository will contain a reference implementation written in Python, but it should be possible to build compatible quasilattice nodes in other languages or protocols other than HTTP.
